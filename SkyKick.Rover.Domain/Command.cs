namespace SkyKick.Rover.Domain
{
    /// <summary>
    /// Enumeration of valid commands, which include Left (L), Right (R), and Move (M)
    /// </summary>
    public enum Command
    {
        L = 1,
        R = 2,
        M = 3
    }
}
